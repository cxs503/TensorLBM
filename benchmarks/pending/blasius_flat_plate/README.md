# B25 平板边界层 Blasius（D2Q9，中置薄板）— ⏳ pending（τ 扫描进行中）

**状态：⏳ 未达标（2026-08-19）：C_f 相对 Blasius 公式 0.664/√Re_x 高 +76~102%，根因定位为低 τ（0.53）近壁格子误差；τ 扫描（τ≥0.65）进行中。**

## 物理问题

均匀流 U 掠过半无限薄平板（前缘 x=x0，无滑移壁面），层流定常边界层。
Blasius 相似解 f''' + f·f''/2 = 0：

- η = (y−y_w)·√(U/(ν(x−x0)))，u/U = f'(η)，f''(0) = 0.33206
- C_f = 2τ_w/(ρU²) = 0.664/√Re_x，Re_x = U(x−x0)/ν

## 配置（中置薄板，2026-08-19 修复版）

- **中置单行薄板**：plate_y = ny/2（域中部），流体双侧，上下镜面（specular）对称边界远离平板
- 前缘台阶修复：早期底行平板把无滑移壁放 y=0.5 而对称面在 y=0，前缘半格"台阶"导致 C_f 系统性 +50%；中置设计消除
- 双侧表面用**流体侧反射 half-way BB**（固体行由相邻流体行构造，含 x 位移，5↔7、6↔8 全反转；周期 stream() 污染固体行自值，标准 post-stream BB / B13 pre-streaming 变体不适用）
- 出口：库 `boundaries.zou_he_outlet_pressure`（ρ=1），τ=0.53 零质量漂移已验证
- 入口：feq 均匀流 Dirichlet 覆写（低 τ 下 zou_he_inlet 反馈发散，见 G-B25-4）
- 网格：plate200（nx=240, ny=1400, le=20, L=200, probe=200）/ plate400（nx=440, ny=1400）/ plate400_y1600（y 加密）
- U=0.05（Ma=0.087）：U=0.1 时 Zou-He 出口在 τ=0.53 下 ~800 步发散（G-B25-5）

## 实测（τ=0.53 基线，30k 步，CPU 32 线程）

| 网格 | C_f_sim | C_f_ref | C_f 误差 | 首格 rel err | 剖面 L2 | u_edge/U |
|------|---------|---------|---------|-------------|---------|----------|
| plate200 | 0.04470 | 0.02213 | **+102.0%** | 45.6% | 0.279 | 1.0005 |
| plate400 | 0.03895 | 0.02213 | **+76.0%** | 27.6% | 0.205 | 1.0010 |
| plate400_y1600 | 0.03895 | 0.02213 | **+76.0%** | 27.6% | 0.204 | 1.0007 |

- 质量漂移 ~0.003-0.004%/30k 步，umax 漂移 ~1e-5，**时间收敛**（非欠收敛假象）
- 剖面误差特征：近壁 η<0.5 内 u_sim/u_ref ≈ **1.276 恒定**（η=0.083/0.25/0.417 处 27.6/27.4/27.0%）——剖面被"拉伸"（薄于 Blasius），有效尺度 ≈0.78× 理论值
- y 加密（1400→1600）不改变误差 → 非域高截断问题；plate200/plate400 差异来自出口距离（plate200 平板尾端距探针仅 20 格）

## 根因与下一步

- **根因（子 agent 诊断）**：τ=0.53 近壁格子（Knudsen）误差——第一格速度系统性偏高 25-28%，壁面 2 阶单侧梯度外推放大 → C_f +76%
- **下一步（进行中）**：τ 扫描 {0.6, 0.65, 0.7, 0.8}（ν=(τ−0.5)/3，固定 U=0.05、probe x=200、ny 随 τ 缩放保持堵塞 ~1.5%）；若某 τ 使 C_f 误差 ≤3%，用板长 200/400 两档网格验证收敛后入库
- 注意：τ 大 → ν 大 → Re_x 减小（900→90），需同时核对有限 Re 对 Blasius 参考的修正（增加固定 τ 长板探针点解耦）

## 共性模块路径

库原语（solver.collide_bgk/stream、d2q9.equilibrium/macroscopic、boundaries.zou_he_outlet_pressure）+ 脚本内自实现边界（镜面 ghost 行、中置薄板双侧流体侧反射 BB、feq 入口）≈30 行。缺口清单见 `references/blasius_gap2.md`（G-B25-1..7）。

## 运行方式

```
PYTHONPATH=src python benchmarks/pending/blasius_flat_plate/run.py /tmp/out.json \
    --grid plate200|plate400|plate400_y1600 --U 0.05 --nu 0.01 --steps 30000 --device cpu
```

τ 扫描脚本：/tmp/blasius_tauscan.py（CPU，8 档，含固定 τ 的 Re_x 解耦点）。
