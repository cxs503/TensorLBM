# 两相 Poiseuille（SC-MCMP 双组分）— 判决记录

日期 2026-09-21。实现：tensorlbm `multiphase.collide_sc_two_component` +
`porous_media` 模块（经 `w4b_lib.py` 编排，零手写物理核）。

## 一句话结论

**FAIL（反收敛）**：H64/H128 中心区最大相对误差 **26.82% / 33.17%**（阈值 ≤3%，
超出 9–11 倍），且**细化恶化**（26.82→33.17，单调判据反向）。误差是 SC-MCMP
在该模型点的**结构性动量耦合偏差**，非 Ma、非互溶度、非驱动强度（归因见下）。

## 判定表（result.json 复算一致）

| 档 | 中心区节点数 | max rel err | mean rel err | u_max 比 | Ma | 判定 |
|---|---|---|---|---|---|---|
| H64 (ny=65) | 55 | 26.82% | 14.21% | 1.26524 | 0.0467 | FAIL |
| H128 (ny=129) | 110 | 33.17% | 15.57% | 1.31458 | 0.1943 | FAIL |
| 单调 | | 26.82 → 33.17 | | | | FAIL（反收敛） |

中心区 = |u| > 0.2·u_max 掩模（两种掩模口径 simmax/anamax 结论相同）。
参考解 = 库自身 `porous_media._two_phase_poiseuille_analytical`（名义黏度比
M=2；未做实测黏度比重标定）。

## 配置

| 项 | H64 | H128 |
|---|---|---|
| τ (water, gas) | (1.0, 0.75) | (1.0, 0.75) |
| G_12 | −2.5（全交换分层） | −2.5 |
| G_x（体积力） | 5e-6 | 5e-6 |
| 名义 M = ν_w/ν_g | 2.0 | 2.0 |
| 界面 | tanh-3（y=half） | 同 |
| 步数 | 60 000 | 180 000 |
| 实测块互溶度 ρ_g-in-water / ρ_w-in-gas | 0.3001 / 0.1593 | 0.3000 / 0.1515 |
| 伪流 \|uy\|_max | 0.0794 | 0.0797 |

## 归因

- **G_x 不变性**：误差比在两种驱动强度下同为 1.265 → 非 Ma/可压缩性。
- **有效黏度比**：因 ~30% 块互溶度，名义 M=4 实测有效 M≈0.54（缺陷 #5）；
  用实测比修正解析只把 u_max 比从 1.265 闭合到 1.24 —— 修正不动 26–33% 的
  剖面偏差，方向也不对。
- **根因（未修，库核心核）**：`collide_sc_two_component` 各分量用**自身**
  速度（非混合物质心速度）构造平衡态 → 两相动量耦合结构性偏差；修复需
  另立项，当前以 xfail-strict 测试锁定证据（PR #305）。

## 库缺陷指针（详见姊妹记录 capillary_invasion_washburn/README.md，共 10 条）

与本记录直接相关：#1 G_12 文档/校验符号错误（分相实需 ≤−2.5，默认 0.9 是
混合模型）；#5 大 τ 对比下有效黏度比反转（互溶度 30%）。

## 文件

- `run.py` — 正式驱动
- `w4b_lib.py` — 共享驱动库（初始化/驱动/测量，物理核全部来自库函数）
- `result.json` — 复算判决（Part 1 切片 + 铁律检查）
- `out_part1.json` — 正式运行原始输出（逐点剖面/收敛史/互溶度）
- `log_part1.txt` — 运行日志
- 完整工件（NOTES、参数探索记录）保留于服务器
  `/nfs/wangxi/runs/bm_widen_w4_20260920/washburn_twophase/`

复现：`python run.py`（需空闲 CUDA 设备，约 10 分钟）。
## 后续核查（2026-09-21）：Shan–Doolen 混合平衡态不恢复本基准

`collide_sc_two_component` 增加 `u_eq="mixture"` 选项（Shan & Doolen 1995
质心速度平衡态；默认 `self`，行为与原实现位同）。该形式恢复了分量间动量
耦合——单碰撞动量交换恒等式 ρ_σ(u_mix−u_σ)/τ_σ、等 τ 总动量守恒、伽利略
不变性、ρ₂→0 单分量极限均验证成立——但本基准在其参数域内仍不达标：

| 运行 | 模式 | G_12 | τ | H | 中心区 max rel err |
|---|---|---|---|---|---|
| R1 / R2 | mixture | −2.5 | (1, 0.75) | 64 / 128 | **发散（NaN @ 第 29 步起）** |
| R1 / R2 | mixture | −1.4 | (1, 0.75) | 64 / 128 | 22.94 / 30.14%（反收敛） |
| R1_t1 | mixture | −2.5 | (1, 1) | 64 | 34.83% |
| R3_t1 | self | −2.5 | (1, 1) | 64 | 7.17% |
| R3 / R4 | self | −2.5 | (1, 0.75) | 64 / 128 | 26.82 / 33.17%（本记录主判决） |

归因更新（替代上文"根因"段的单因子表述）：

- **不等 τ 的混合格式在强分相下结构性不稳定**：每步净动量源
  (1/τ₁−1/τ₂)·ρ₁ρ₂(u₁−u₂)/(ρ₁+ρ₂)（等 τ 时归零）。G≤−1.5 时全 NaN
  （−2.5/−2.0/−1.5 → 第 29/46/175 步），与驱动强度和初始化无关。
- **稳定域内主导残差=界面条带化**：平界面碎裂为水/气交替条带
  （min ρ_tot 0.585–0.816），黏度分层被打乱——等 τ 档 mixture 34.83%
  vs self 7.17% 佐证（等 τ 时混合与自速的名义差异已消）。
- 分量自速度平衡态（原根因注记）是偏差的必要成分而非充分解释：混合
  平衡态落地后，残差转移为界面条带化主导。判决维持 FAIL。

过程工件：`/nfs/wangxi/runs/bm_widen_w5_20260921/mcmp_fix/`（含判别测试
`tests/test_mcmp_mixture.py` 与 11 个运行 JSON）。
