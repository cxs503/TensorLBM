"""Production middleware (stub — TODOs commented out)."""

from fastapi import FastAPI


def install_production_middleware(app: FastAPI) -> None:
    """Install auth, rate-limit, gzip, metrics middleware (stub)."""
    # TODO: add real middleware when ready
    pass
